# IDEs and Editors
# ------------------------------------
# IntelliJ/WebStorm/etc.
.idea
*.iml
*.iws
*.ipr
# VS Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.history

# Node.js
# ------------------------------------
# Dossiers de dépendances générés
/node_modules
# Fichier de lock généré par Yarn
/yarn.lock
# Fichier de lock généré par npm
/package-lock.json
# Fichier de lock généré par pnpm
/pnpm-lock.yaml

# Build Artifacts (Sorties de Compilation)
# ------------------------------------
# Les fichiers générés par le processus de 'build' (Vite, Webpack, Vue CLI)
# C'est le dossier qui contient la version prête à être déployée.
/dist
/build

# Logs et Données Temporaires
# ------------------------------------
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.eslintcache
.nuxt # Si vous utilisez Nuxt.js
.vite # Dossiers temporaires de Vite
.rollup.cache # Cache de Rollup (utilisé par Vite)
/coverage # Rapport de couverture de tests (ex: Vitest, Jest)

# Fichiers Temporaires du Système d'Exploitation
# ------------------------------------
.DS_Store # macOS
Thumbs.db # Windows

# Fichiers de Configuration Sensibles
# ------------------------------------
# Fichiers contenant des clés API, des mots de passe, etc.
# NE JAMAIS COMMITER CES FICHIERS !
.env
.env.*
!.env.example # Permet de commiter un fichier d'exemple pour les autres développeurs